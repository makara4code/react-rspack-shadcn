import { RouterProvider } from 'react-router-dom';
import './App.css';
import { Button } from "federation_provider/components";

const App = () => {
  return (
   <RouterProvider router={Router} />
  );
};

export default App;